
name: Check white_album
on:
  push:
    branches:
      - main
    paths:
      - 'services/white_album/**'
      - 'checkers/white_album/**'
  workflow_dispatch:
    inputs:
      cleanup_before_deploy:
        description: 'Stops and fully removes service (with volumes!) before deploying again. Type "yes" to do it.'
        required: false
        default: "no"

jobs:
  check_white_album:
    name: Check white_album
    runs-on: ubuntu-18.04

    steps:
    - name: Checkout repo
      uses: actions/checkout@v2

    - name: Run prebuilt hook
      run: if [ -f services/white_album/before_image_build.sh ]; then (cd ./services/white_album && ./before_image_build.sh); fi

    - name: Setup white_album
      run: (cd ./services/white_album && docker-compose pull && docker-compose build && docker-compose up --build -d)

    - name: Prepare python for checker
      uses: actions/setup-python@v2
      with:
        python-version: 3.8.5

    - name: Setup checker libraries
      run: if [ -f checkers/white_album/requirements.txt ]; then python -m pip install -r checkers/white_album/requirements.txt; fi

    - name: Test checker on service
      run: checkers/white_album/checker.py TEST 127.0.0.1

  update_white_album:
    name: Deploy service using ansible to first teams
    needs: check_white_album
    runs-on: ubuntu-18.04
    if: ${{ false }} # disable on game just in case

    steps:
    - name: install ansible
      run: sudo apt-get install -y ansible

    - name: Checkout repo
      uses: actions/checkout@v2

    - name: change permission for ssh key
      run: chmod 0600 ./vuln_image/keys/id_rsa

    - name: Run prebuilt hook
      run: if [ -f services/white_album/before_image_build.sh ]; then (cd ./services/white_album && ./before_image_build.sh); fi

    - name: stop white_album, destroy volumes and cleanup service before fresh deploy
      if: ${{ github.event.inputs.cleanup_before_deploy == 'yes' }}
      run: ./vuln_image/cleanup_first_ten_teams.sh white_album

    - name: try to deploy white_album
      run: ./vuln_image/update_first_ten_teams.sh white_album

