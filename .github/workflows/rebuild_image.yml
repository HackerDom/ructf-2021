name: Rebuild image
on:
  workflow_dispatch: {}

jobs:
  rebuild_image:
    name: Rebuild and export image
    runs-on: buildimage

    steps:
    - name: try to rebuild image
      run: echo pwd && ls && packer build  -var "api_token=$(cat /root/do-token)" vuln_image/image.pkr.hcl
